# Secret-scanner suppressions for INTENTIONAL benchmark content.
# Every entry below is a deliberately planted weak default, demo credential, or
# piece of fake PII used by exploit proofs — not a leaked secret. See
# benchmarks/ground_truth.yaml (CF05, known_unlabeled) for the rationale.

# Hardcoded dev-secret defaults (planted finding CF05)
langfail/core/config.py

# Seeded demo users admin/admin123, alice/alice123, bob/bob123 (documented in README)
langfail/cli.py

# Fixed JWT secret used by the test fixtures
tests/conftest.py

# Fake customer PII (SSN/card/emails) in the seeded fine-tune corpus used by
# the training-data-extraction proof (V62)
langfail/__init__.py

# --- mutant suite (modelbay corpus) -- the same intentional content, mutated.
# See docs/adr/0001-blinded-mutation-benchmark.md.

# Hardcoded dev-secret defaults (the mutant's CF05 analogue)
modelbay/core/config.py

# Fake customer PII in the seeded transcript corpus used by V62's proof
modelbay/__init__.py
